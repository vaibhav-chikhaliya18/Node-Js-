<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Store</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar {
            width: 100%;
            background: #EDF4F2;
            color: #38413b;
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .navbar h2 {
            margin-left: 10%;
            font-size: 1.8rem;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .navbar div {
            display: flex;
            gap: 10px;
            margin-right: 10%;
        }

        .navbar a {
            color: #38413b;
            text-decoration: none;
            padding: 8px 15px;
            border-radius: 5px;
            font-size: 1.2rem;
            font-weight: 600;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            transition: background-color 0.3s, transform 0.2s;
        }

        .navbar a:hover {
            background-color: rgba(255, 255, 255, 0.2);
            transform: scale(1.1);
        }

        .navbar a:active {
            background-color: rgba(255, 255, 255, 0.3);
        }


        .movie-card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            text-align: center;
            max-width: 100%;
            margin: 0 auto;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .movie-card:hover {
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
        }

        .card-body {
            text-align: left;
            margin-left: 3%;
        }

        .movie-card img {
            width: 100%;
            height: 300px;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .movie-card:hover img {
            transform: scale(1.05);
        }

        .card-title {
            font-size: 1.2rem;
            margin: 10px 0;
            font-weight: bold;
            color: #38413b;
        }

        .card-genre {
            font-weight: 600;
            margin-bottom: 10px;
            color: #38413b;
        }


        .card-rating {
            font-size: 1rem;
            color: #38413b;
            font-weight: bold;
        }

        .movie-card .card-body {
            position: relative;
            overflow: hidden;
        }
    </style>
</head>

<body>
    <div>
        <div class="navbar">
            <h2 align="center">Movie Dashboard</h2>
            <div>
                <a href="/">Home</a>
                <a href="/add">Add Movie</a>
                <a href="/view">View Movies</a>
            </div>
        </div>
        <div class="container">
            <header style="margin: 2% 0;">
                <h2 align="center">✨ View Movies ✨</h2>
            </header>

            <div class="row">
                <% detail.map((val)=> { %>
                    <div class="col-md-3 mb-4">
                        <div class="card movie-card">
                            <img src="<%= val.image %>" alt="<%= val.name %>">
                            <div class="card-body">
                                <h5 class="card-title">Name : <%= val.name %>
                                </h5>
                                <p class="card-genre">Description : <%= val.description %>
                                </p>
                                <p class="card-rating">Price : ₹<%= val.price %>
                                </p>
                                <div class="d-block">
                                    <a href="/delete?deletId=<%= val.id %>" class="btn btn-danger" style="padding: 10px 40px; font-size: 20px;">Delete</a>
                                    <a href="/edit?editId=<%= val.id %>" class="btn btn-primary" style="padding: 10px 40px; font-size: 20px;">Edit</a>
                                </div>
                            </div>
                        </div>

                    </div>
                    <% }) %>
            </div>
        </div>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>