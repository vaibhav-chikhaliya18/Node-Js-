<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Movie</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(105deg, rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), url('https://akm-img-a-in.tosshub.com/businesstoday/images/story/202305/ezgif-sixteen_nine_241.jpg');
            background-size: cover;
            background-position: center;
            height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Poppins', sans-serif;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.9); 
            padding: 30px 40px;
            border-radius: 15px;
            max-width: 500px;
            width: 100%;
        }

        .form-control {
            border-radius: 10px;
            box-shadow: none;
            height: 40px;
        }

        textarea.form-control {
            height: 150px;
        }

        .form-label {
            font-weight: 600;
            margin-bottom: 10px;
        }

        h1 {
            color: #6C3E2D;
            font-size: 32px;
            text-align: center;
            margin-bottom: 30px;
            font-weight: 600;
        }

        button {
            font-size: 16px;
            font-weight: 600;
            padding: 12px;
            border-radius: 10px;
            border: none;
        }

        button.btn-success {
            background-color: #28a745;
            color: white;
        }

        button.btn-success:hover {
            background-color: #218838;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-control:focus {
            border-color: #28a745;
            box-shadow: 0 0 0 0.25rem rgba(40, 167, 69, 0.25);
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="my-4">Add New Movie</h1>
        <form action="/add" method="POST">
            <div class="mb-3 form-group">
                <label for="movie_image" class="form-label">Movie Image URL</label>
                <input type="text" class="form-control" id="movie_image" name="movie_image" required>
            </div>
            <div class="mb-3 form-group">
                <label for="movie_name" class="form-label">Movie Name</label>
                <input type="text" class="form-control" id="movie_name" name="movie_name" required>
            </div>
            <div class="mb-3 form-group">
                <label for="description" class="form-label">Description</label>
                <textarea class="form-control" id="description" name="description" required></textarea>
            </div>
            <div class="mb-3 form-group">
                <label for="price" class="form-label">Price</label>
                <input type="number" class="form-control" id="price" name="price" required>
            </div>
            <button type="submit" class="btn btn-success w-100">Add Movie</button>
        </form>
    </div>
</body>
</html>
