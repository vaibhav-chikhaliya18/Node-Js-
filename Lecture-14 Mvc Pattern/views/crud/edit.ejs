<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <div align="center">
        <h1>Edit Page of Crud</h1>

        <form action="/crud/updaterecord" enctype="multipart/form-data" method="post">
            <input type="hidden" name="editId" value="<%= singleRow.id %>">
            <table border="1">
                <tr>
                    <td>Name :- </td>
                    <td>
                        <input type="text" name="name" value="<%= singleRow.username %>">
                    </td>
                </tr>
                <tr>
                    <td>Email :-</td>
                    <td>
                        <input type="text" name="email" value="<%= singleRow.useremail %>">
                    </td>
                </tr>
                <tr>
                    <td>Password :-</td>
                    <td>
                        <input type="text" name="password" value="<%= singleRow.userpassword %>">
                    </td>
                </tr>
                <tr>
                    <td>Gender :-</td>
                    <td>
                        <input type="radio" name="gender" value="Male" <%= singleRow.usergender === "Male" ? "checked" : "" %>>Male
                        <input type="radio" name="gender" value="Female" <%= singleRow.usergender === "Female" ? "checked" : "" %>>Female
                    </td>
                </tr>
                <tr>
                    <td>Hobby :-</td>
                    <td>
                        <input type="checkbox" name="hobby" value="Playing" <%= singleRow.userhobby.includes("Playing") ? "checked" : "" %>>Playing
                        <input type="checkbox" name="hobby" value="Singing" <%= singleRow.userhobby.includes("Singing") ? "checked" : "" %>>Singing
                        <input type="checkbox" name="hobby" value="Dancing" <%= singleRow.userhobby.includes("Dancing") ? "checked" : "" %>>Dancing
                    </td>
                </tr>
                <tr>
                    <td>City :-</td>
                    <td>
                        <select name="city" id="">
                            <option value="">--- Select City ---</option>
                            <% city=["Surat", "Amreli" , "Vadodara" , "Rajkot" , "Jamnagar" ] 
                                city.map((val,index)=> { %>
                                <option value="<%= val %>" <%= singleRow.usercity === val ? "selected" : "" %>>
                                    <%= val %>
                                </option>
                                <% })%>
                        </select>

                    </td>
                </tr>
                <tr>
                    <td>File :-</td>
                    <td>
                        <input type="file" name="fileimage">
                        <img src="/<%= singleRow?.userimage %>" width="100" alt="">
                    </td>
                </tr>
                <tr>
                    <td></td>
                    <td>
                        <input type="submit">
                    </td>
                </tr>
            </table>
        </form>
        <a href="/crud">View</a>
    </div>
</body>

</html>